<Window x:Class="WpfChartDemo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfChartDemo"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        mc:Ignorable="d"
        Title="Game User Analysis - WPF Chart Demo" Height="600" Width="1200">
    
    <Window.DataContext>
        <local:InitViewModel/>
    </Window.DataContext>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- CSV Load Button and Status -->
        <DockPanel Grid.Row="0" Margin="10" LastChildFill="True">
            <Button DockPanel.Dock="Left" 
                    Content="CSV読み込み" 
                    Command="{Binding LoadCsvCommand}"
                    Padding="10,5"
                    Margin="0,0,10,0"/>
            <TextBlock Text="{Binding StatusMessage}" 
                       VerticalAlignment="Center"
                       Foreground="DarkBlue"/>
        </DockPanel>
        
        <!-- Line Chart for Monthly Sales by Gender -->
        <GroupBox Grid.Row="1" Header="Line Chart - Monthly Sales by Gender" Margin="10">
            <lvc:CartesianChart Series="{Binding LineSeries}" 
                                XAxes="{Binding LineXAxes}"
                                YAxes="{Binding YAxes}"
                                Title="{Binding LineChartTitle}"
                                LegendPosition="Right">
            </lvc:CartesianChart>
        </GroupBox>
        
        <!-- Bar Chart for Monthly Spending by Country -->
        <GroupBox Grid.Row="2" Header="Bar Chart - Monthly Spending by Country" Margin="10">
            <lvc:CartesianChart Series="{Binding BarSeries}" 
                                XAxes="{Binding XAxes}"
                                YAxes="{Binding YAxes}"
                                Title="{Binding BarChartTitle}"
                                LegendPosition="Right">
            </lvc:CartesianChart>
        </GroupBox>
    </Grid>
</Window>
